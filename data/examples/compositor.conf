# compositor.conf — xfce4-wayland compositor settings
#
# Place in the configuration directory passed via
# `xw-compositor -c DIR` (xw-session uses $XDG_CONFIG_HOME/xfce4-wayland).
# Every key is optional; defaults are shown.

[background]
# Desktop background fill, 0xAARRGGBB.
color = 0xff202530
